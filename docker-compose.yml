version: '3.8'

services:
  express-app:
    container_name: stokinventory-api
    build: .
    ports:
      - "4005:4005"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
networks:
  web:
    external: true